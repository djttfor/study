server:
  port: 80

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/base?characterEncoding=UTF8
    username: root
    password: 1998

logging:
  level:
    org:
      springframework:
        boot:
          autoconfigure:
            logging: info
