package ${packageName};

import org.springframework.web.bind.annotation.RequestMapping;

import org.springframework.stereotype.Controller;

/**
 * <p>
 *
 * </p>
 *
 * @author ${author}
 * @since ${date?string("yyyy-MM-dd")}
 */
@Controller
@RequestMapping("/${accessPath}")
public class ${controllerName} {
}